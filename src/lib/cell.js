export default class Cell {
    constructor(x, y, width, height, vector) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
        this.vector = vector;
    }
}